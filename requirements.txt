fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.6.0
faster-whisper>=1.0.0
supabase>=2.10.0
python-dotenv>=1.0.0
httpx>=0.27.0
anthropic>=0.92.0
slowapi>=0.1.9
# Beat Sync: beat detection
librosa>=0.10.0
soundfile>=0.12.0
# Background Removal (AI mode)
rembg>=2.0.0
Pillow>=10.0.0
# ffmpeg-python is NOT needed — we call ffmpeg via subprocess directly
# Optional: self-hosted translation (set LIBRETRANSLATE_URL in .env)
# LibreTranslate: https://github.com/LibreTranslate/LibreTranslate
